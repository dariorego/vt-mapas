version: '3.8'

services:
  app:
    build: .
    container_name: vt-mapas-app
    restart: always
    ports:
      - "80:80"
    environment:
      - DB_HOST=seu_db_host
      - DB_NAME=prod_vt
      - DB_USER=seu_usuario
      - DB_PASS=sua_senha
    volumes:
      - ./data:/var/www/html/data
